spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/data_task2?serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.datatask2.entity
mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8086
